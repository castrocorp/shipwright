# Code Quality

- Read `~/.claude/stacks/<stack>.md` for language-specific rules (stack declared in `.claude/project.md`)
- Run lint check on YOUR files only (command from `.claude/project.md`)
- NEVER run lint format if `.claude/project.md` marks it FORBIDDEN
- NEVER fix pre-existing violations in unrelated files
