# Branch Management

- ALWAYS branch from the base branch declared in `.claude/project.md` (never main/master unless configured)
- Branch naming convention from `.claude/project.md`
- Check JIRA ticket type to determine prefix (bug -> `fix/`, other -> `feat/`)
